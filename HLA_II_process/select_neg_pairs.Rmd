---
title: "select_neg_pairs"
author: "Fumin Li"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
neg_pair_II <- data.frame()
for (i in 1:nrow(sig_pair_II)) {
  replicated_rows <- sig_pair_II[rep(i, 5), ]
  
  current_count <- sig_pair_II$count[i]
  
  matching_rows <- tcr_count[tcr_count$count == current_count, ]
  sampled_rows <- matching_rows[sample(nrow(matching_rows), 5, replace = TRUE), ]

  combined_rows <- cbind(replicated_rows[, 6], sampled_rows[,])
  
  neg_pair_II<- rbind(result, combined_rows)
}
neg_pair_II <- neg_pair_II[,c(5,3,1)]
colnames(neg_pair_II) <- c("v", "tcr", "hla")
```



